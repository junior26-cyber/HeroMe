# 📤 Guide pour Pousser sur GitHub

## Étape 1 : Créer un repository sur GitHub

1. Allez sur [github.com](https://github.com)
2. Connectez-vous à votre compte
3. Cliquez sur **"New"** pour créer un nouveau repository
4. **Nom du repository :** `herome` (ou ce que vous préférez)
5. **Description :** `🦸 Application Flutter de recherche de superhéros`
6. **Visibilité :** Public
7. **Ne cochez PAS** "Initialize this repository..." (on a déjà un repo local)
8. Cliquez **"Create repository"**

---

## Étape 2 : Ajouter la remote GitHub

Après la création, GitHub affichera les commandes. Exécutez :

```bash
# Ajouter la remote (remplacez USERNAME et REPO)
git remote add origin https://github.com/YOUR_USERNAME/herome.git

# Vérifier que c'est correct
git remote -v
```

---

## Étape 3 : Pousser le code

```bash
# Push le branch main vers GitHub
git branch -M main
git push -u origin main
```

---

## Étape 4 : Vérifier sur GitHub

Allez sur `https://github.com/YOUR_USERNAME/herome` et confirmez que le code est là ! 🎉

---

## 📝 Commandes Utiles pour Futurs Commits

```bash
# Voir le statut
git status

# Ajouter tous les changements
git add .

# Commit avec un message
git commit -m "feat: add new feature"

# Pousser vers GitHub
git push
```

---

## 🔐 Authentification SSH (optionnel mais recommandé)

Pour éviter de saisir le mot de passe chaque fois :

1. **Générer une clé SSH :**
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. **Ajouter à l'agent SSH :**
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

3. **Copier la clé publique :**
```bash
cat ~/.ssh/id_ed25519.pub
```

4. **Ajouter sur GitHub :**
   - Settings > SSH and GPG keys > New SSH key
   - Coller la clé et confirmer

5. **Modifier l'URL de la remote :**
```bash
git remote set-url origin git@github.com:YOUR_USERNAME/herome.git
```

---

## ✅ C'est prêt !

Votre projet est maintenant sur GitHub ! 🚀
